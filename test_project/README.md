# test_project

This project was bootstrapped using **projinit**.

---

## ⚙️ Environment Setup

Create a new conda environment:

```bash
conda create -n ty python=3.22 -y
```

Activate the environment:

``` bash
conda activate ty
```
📦 Install Dependencies
```bash

pip install -r requirements.txt
```
▶️ Run the Project
```bash

python run.py
```
🧪 Run Tests (optional)
```bash

pytest
```